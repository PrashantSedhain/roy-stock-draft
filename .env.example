# Use this to validate the complete app locally without consuming market-data credits.
USE_MOCK_DATA=true

# For real data, remove USE_MOCK_DATA and configure one provider.
# MARKETDATA_API_TOKEN=your_token
# FINNHUB_API_KEY=your_key
